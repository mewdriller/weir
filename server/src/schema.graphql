# import Organization, Project from "./generated/prisma.graphql"

type Query {
  organizations: [Organization!]!
  project(handle: String!): Project
  projects: [Project!]!
}

type Mutation {
  createProject(
    handle: String!
    name: String!
    organizationId: String!
  ): Project
}
